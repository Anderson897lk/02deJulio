server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Anderson897lk/02deJulio.git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: Cherman
      password: 202211951
